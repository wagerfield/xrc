import React from "react"
import { Box } from "xrc"

export const Wrapper = (props) => (
  <Box
    size="1"
    padding="4"
    display="flex"
    alignItems="center"
    justifyContent="center"
    {...props}
  />
)
